#!/usr/bin/env sh

if [ "$_" = "$0" ]; then
    echo "$0 must be sourced"
elif [ -z "${1}" ]; then
    >&2 echo "a path is required"
else
    export PATH=$(echo ${PATH} | sed -E -e "s;:${1};;" -e "s;${1}:?;;")    
fi
