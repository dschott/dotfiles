#!/usr/bin/env sh

[ -z "$(command -v code)" ] && exit

if [ "$(uname -s)" == "Darwin" ]; then
    cp "$DOTPATH/etc/vscode/settings.json" "$HOME/Library/Application Support/Code/User/settings.json"
elif [ "$(uname -s)" == "Linux" ]; then
    cp "$DOTPATH/etc/vscode/settings.json" "$HOME/.config/Code/User/settings.json"
fi
