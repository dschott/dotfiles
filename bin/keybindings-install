#!/usr/bin/env sh

echo "installing mac os default keybindings"

mkdir -p "${HOME}/Library/KeyBindings/"
ln -fs "${DOTPATH}/etc/DefaultKeyBinding.dict" "${HOME}/Library/KeyBindings/DefaultKeyBinding.dict"
