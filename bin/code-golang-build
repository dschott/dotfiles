#!/usr/bin/env sh

[ -z "$(command -v code)" ] && exit

[ ! -d "${GOPATH}/src/github.platforms.engineering/graphroots/golang-build" ] && exit

code "${GOPATH}/src/github.platforms.engineering/graphroots/golang-build"
